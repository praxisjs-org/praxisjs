---
"create-praxisjs": patch
---

Scoped package names (`@org/pkg`) are now preserved correctly during scaffolding. Template and `_package.json` errors report actionable messages instead of raw exceptions.
