---
"@praxisjs/storybook": patch
---

Added the missing `sourceUrl` and `expanded` fields to `StorybookRefs`, matching the options documented for [Storybook Composition](https://storybook.js.org/docs/sharing/storybook-composition) refs.
