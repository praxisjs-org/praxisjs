---
"create-praxisjs": patch
---

Update project templates to reflect the decorator-first API: use `@Store`, `@Route`, and `@RouterConfig` decorators, and initialize DevTools via dynamic import after `render()`.
