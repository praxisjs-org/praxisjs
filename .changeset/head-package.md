---
"@praxisjs/head": minor
---

Initial release of `@praxisjs/head` — reactive document head management via `@Head` class decorator. Supports title, description, canonical, arbitrary meta[], og:*, and twitter:*. Stack-based with automatic cleanup on component unmount.
