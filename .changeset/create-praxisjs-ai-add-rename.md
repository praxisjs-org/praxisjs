---
"create-praxisjs": patch
---

Updated dependency — `praxisjs@1.0.0`. The AI integration command referenced after scaffolding (previously `praxisjs add`) is now `praxisjs ai add`; see the [praxisjs CLI docs](https://praxisjs.org/docs/tooling/cli) for the full command reference, including the new `ai remove`, `doctor`, and `upgrade` commands.
