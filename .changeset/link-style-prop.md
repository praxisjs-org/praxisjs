---
"@praxisjs/router": patch
---

Add `style` prop support to `Link`. Accepts both a CSS string (`style="color:red"`) and an object (`style={{ color: 'red' }}`), matching the standard JSX element behavior.
