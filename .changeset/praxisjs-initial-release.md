---
"praxisjs": minor
---

Initial release — CLI for maintaining existing PraxisJS projects. Ships `praxisjs add`, which installs an AI integration (Claude Code or Codex skill) into an existing project; this command previously lived in `create-praxisjs`.
