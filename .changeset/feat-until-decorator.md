---
"@praxisjs/decorators": minor
---

Add `@Until(propName)` decorator. Replaces the decorated method with one that returns a `Promise` resolving to the first truthy value of the named signal or computed property.
